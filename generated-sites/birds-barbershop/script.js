// The scroll-scrubbed hero needs zero JS (background-position + steps() +
// animation-timeline: scroll(), per the RESOLVED Flipbook Alternative
// finding in vault/Projects/Website Generator.md). Nothing else on this
// first pass needs client-side behavior - the contact form isn't wired to
// a live destination yet (see index.html's contact section comment), so
// there's no submit handler to write until that's decided.
