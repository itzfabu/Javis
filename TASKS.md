# Tasks

Format: - \[ ] Task text | priority: high/medium/low | due: YYYY-MM-DD | #project-tag

## Active

- [ ] Write up "Claude Flowstate" idea as a note | priority: low

- [ ] Research how to improve the Jarvis system further (architecture, tools, capabilities, possible new dashboards e.g. a finance dashboard for project income once there's income to track) | priority: medium | #jarvis-system
  - Input ready for this one: vault/Ideas/Multi-Page Orb Dashboard.md (multi-page dashboard concept + notes-panel cleanup) and vault/Ideas/Finance Dashboard for Project Income.md - start here tomorrow morning.

- [ ] Fix Thread 1's font-substitution logic: `googleFontsUrl()`/`resolveFont()` (tools/brand-extraction/src/fonts.js) always requests the caller's weight list (e.g. `[500,700]` for headings) regardless of whether the resolved Google Font actually ships those weights | priority: medium | #website-generator
  - NEW 2026-08-07: found fixing Birds Barbershop's fonts (real page build follow-up) - the fixture's own tokens.json requested `Righteous:wght@500;700`, which 400s (Google: "Missing font family") because Righteous only ships weight 400. The old external `<link>` version of the page was silently failing to load its heading font this whole time and falling back to plain sans-serif - a failed stylesheet `<link>` throws no visible error, so this was never caught. Fixed locally for this one page (self-hosted at the correct weight 400 only) but the root cause is in `fonts.js` itself and likely affects other single-weight display fonts in `COMMERCIAL_FONT_MAP`/`GOOGLE_FONTS_KNOWN` (Bebas Neue is a plausible second case, unchecked). Real fix needs a per-font available-weights table to clamp requested weights, not a per-fixture patch.

- [x] Small Thread 1 schema addition: promote `category` to a first-class token field (e.g. "category": "food-beverage") instead of leaving it inside archetype.reason free text | priority: low | #website-generator
  - FOUND ALREADY DONE 2026-08-07: checked `tools/brand-extraction/src/tokens.js` directly while building the deterministic assembly step (which needs to read `tokens.category` itself) - `buildTokens()` already emits `category` as a first-class top-level field (line 135), confirmed present in both the Birds Barbershop and Grace Family Roofing token fixtures. Closing as done rather than leaving it open against stale information; unclear which session actually made this change.

- [x] Build Thread 2's page template: one real generated page, end to end (first pass, above-the-fold + a disclosed-incomplete below-fold) | priority: medium | #website-generator
  - DONE 2026-08-07: `generated-sites/birds-barbershop/` - real static index.html/styles.css/script.js/assets/meta.json, consuming the real TRANSFORM/Birds Barbershop Thread 1 fixture and its Thread 3 sprite output (post quality-fix, see the TRANSFORM entry above). Found and fixed two real bugs building an actual page instead of just the isolated hero: `src/static-server.js` had no `.css` MIME type (silently served stylesheets as octet-stream, Chromium refused to apply them - fixed, added); and the first draft put hero copy inside `.hero-sticky` alongside the sprite, overflowing the 100vh sticky box and clipping the headline (fixed - hero copy moved to its own static section above `.hero-track`). Below the hero is deliberately incomplete, not a shortcut: this fixture has real brand tokens but no intake-form facts (NAP, reviews, photos) for the mandatory trust bar / social proof / gallery sections, and inventing them for a real business (birdsbarbershop.com) would misrepresent it - omitted per Thread 2's own client-supplied-or-omitted rule, all gaps disclosed in meta.json's `known_gaps` and in-page HTML comments. Not wired into orb/app.py's `/generate-website` route - see new task below for why. Full record in vault/Projects/Website Generator.md, Thread 2 > BUILD (2026-08-07).
- [x] Wire Thread 1+3's real pipeline into orb/app.py's `/generate-website` route (currently a fully separate LLM-freeform generator with zero Thread 1/3 integration) | priority: medium | #website-generator
  - NEW 2026-08-07: deferred, not forgotten. The current route prompts an LLM to write index.html/styles.css/script.js from scratch per request - no tokens, no sprite, no skeleton - which directly contradicts Thread 1's own "deterministic script, not LLM improvisation" architecture note. Wiring the real pipeline in means building a real deterministic assembly step (Thread 1 extraction -> Thread 3 sprite generation -> template assembly from tokens), not a config change. Deferred until the Web3Forms account and intake-form gaps below are resolved - hard-wiring a route around unresolved decisions would bake them into every future generated site instead of catching them once, the same reason the two real bugs above were worth finding by hand-building first.
  - RESOLVED 2026-08-07: built `tools/site-assembly/` (Thread 1 extraction -> Thread 3 sprite generation -> font self-hosting -> template assembly, category-driven copy from Thread 2's own variant table, not an LLM call) and wired it into `orb/app.py`'s `/generate-website` as `run_deterministic_generation_job`, used whenever `reference_url`+`is_own_site`+`category` are all given (the LLM-freeform path is unchanged and still used otherwise - disclosed, since Thread 1 has no other working extraction input path yet). Proved it by generating a SECOND real site end to end through the actual running route: Grace Family Roofing (ASSEMBLE, construction-trades), live extraction from gfryork.com, not a canned fixture. Found and fixed three real bugs a second archetype was always going to surface: (1) the logo filename was hardcoded to `.jpg` in the template - Grace Family Roofing's is a `.png` - now the real extension is passed through instead of assumed; (2) this client's extracted accent color is `#FFFFFF`, identical to its background - a real Thread 1 extraction-quality issue, not fixed by guessing a better color, mitigated generically by giving every `.btn` a visible border so it stays legible as a shape regardless of how degenerate a client's accent/background pairing is; (3) the real blocker - the frustum/occlusion guards in `tools/frame-generation/src/capture.js` ran against an empty logo texture for this fresh (unreviewed) extraction and failed loudly ("0 opaque pixels"), which would have blocked every first-time generation, not just this one - fixed by skipping both guards when nothing is actually being composited, not by loosening them for the case where a real logo needs checking. Full record in vault/Projects/Website Generator.md, Thread 2 > FONT SELF-HOSTING + DETERMINISTIC ASSEMBLY (2026-08-07).
- [ ] Create a Web3Forms account for generated-site contact forms (external-service decision, needs sign-off - not created without asking first) | priority: medium | #website-generator
  - NEW 2026-08-07: surfaced building the Birds Barbershop page - Thread 2's own form-backend decision (Web3Forms now, DECIDED 2026-08-05) needs an actual account + access key before any generated site's contact form can submit anywhere. The Birds Barbershop page's form is left disclosed-as-not-wired (action="#", commented) rather than pointed at a placeholder. Per this project's standing rule, no external account gets created without asking first.
  - BLOCKED 2026-08-07 (approved, attempted, could not complete): signup at web3forms.com ("Create your Form") is gated behind a Cloudflare "Verify you are human" checkbox before email verification even starts - completing CAPTCHAs/bot-detection is outside this agent's allowed actions regardless of task-level approval. `tools/site-assembly/src/assemble.js` already reads `WEB3FORMS_ACCESS_KEY` from the environment (same `orb/.env` convention `OPENAI_API_KEY` already uses) - wiring the key in once it exists is a one-line `.env` addition, not more engineering. Needs Fabio to either complete the signup himself and hand back the access key, or name a form-backend service without a CAPTCHA gate.
- [ ] Thread 1's extraction pipeline doesn't capture the intake-form facts Thread 2's skeleton assumes exist (NAP, reviews, before/after photos, years-in-business) | priority: low | #website-generator
  - NEW 2026-08-07: surfaced by the Birds Barbershop build - a real Thread 1 token fixture has brand tokens (color/font/logo) but nothing from a client intake form, so Thread 2's mandatory trust bar and social proof sections and the category-conditional gallery had nothing to render and were correctly omitted, not filled with placeholders. This is a real Thread 1<->Thread 2 handoff gap, not a Thread 2 design flaw - Thread 1 needs an intake-form field set (or Thread 2 needs its own separate intake step) before a generated site can ship those sections for a real client.
- [ ] Re-judge sprite LCP budgets against a real page, not the isolated-hero test harness - TRANSFORM's real margin is ~260ms, not the 708-740ms the isolated reading showed | priority: medium | #website-generator
  - NEW 2026-08-07: every LCP figure on this board (REVEAL, ASSEMBLE, FLYTHROUGH, TRANSFORM) was measured with the sprite alone on a blank test page. Measured the real Birds Barbershop page instead (test/measure-lcp-real-page.js, 4x CPU + Slow-4G, 15 trials, control included): the hero-sprite's own render time is ~448ms slower on the real page (2240ms median, Element Timing) than the isolated reading taken minutes earlier in the same session (1760/1792ms). TRANSFORM is still Good, but the real margin (~260ms) is thin again, not the comfortable buffer the quality fix above appeared to buy back - and this page is still missing the trust bar/social proof/gallery sections a fuller real site would carry, so ~260ms is likely optimistic, not final. REVEAL/ASSEMBLE/FLYTHROUGH's open LCP-gap tasks above are still judged against the isolated harness only - re-measuring them against a real page (once one exists per archetype) should be done before treating any of those verdicts as final either. Full data in vault/Projects/Website Generator.md, Thread 2 > BUILD (2026-08-07).
  - UPDATED 2026-08-07 (fonts self-hosted, re-measured): tested the obvious hypothesis - that the fonts.googleapis.com/fonts.gstatic.com round-trips were a real chunk of the ~448ms - by self-hosting the fonts and re-measuring the same way. **Wrong hypothesis, measured directly, not left standing on assumption: fonts accounted for ~0ms of it.** Post-fix hero-sprite render time is 2216-2260ms across two 15-trial runs, statistically flat against the pre-fix 2240ms. Font self-hosting was still worth doing (see the DONE entry above - it fixed a real bug, a broken Righteous weight request that was silently failing the whole time) but the element it sped up (the H1 headline, ~130ms faster) was never the page's actual LCP element, so the fix doesn't show up in the number that matters. Decomposed the real ~448ms via Resource Timing instead of guessing again: vs. the isolated harness's own transfer/decode split (1135ms/425ms, same session), the real page adds ~308ms of **discovery delay** (more HTML/CSS the browser parses before it finds the sprite's background-image and starts fetching it) and ~253ms of **transfer slowdown** (the sprite now shares the throttled 1.6Mbps link with styles.css, script.js, the logo, and 4 font files, instead of being nearly the only request on the page) - decode was actually *faster* on the real page (-114ms), read as noise per this project's own established finding that decode is a small, session-noisy number, not a third real contributor. Neither discovery nor transfer delay is fixable by trimming the sprite itself - the ~260ms margin stands, now confirmed measured against real, structural page-weight cost rather than an easily-removed one. Full data in vault/Projects/Website Generator.md, Thread 2 > BUILD (2026-08-07) font-hosting follow-up.

- [ ] Close REVEAL's remaining LCP gap (measured: 3.09s no-logo / 3.21s approved-logo, ~0.6-0.7s over the 2.5s Good threshold) | priority: medium | #website-generator
  - UPDATED 2026-08-06: re-measured with real CDP network throttling (Slow-4G, replacing the old file-size/200KB/s estimate) - gap is LARGER than the estimate suggested (0.59-0.71s over vs. the old 0.41-0.53s), not smaller. Estimate-vs-measured verdict bucket is unchanged (still Needs Improvement, still doesn't clear Good) - per the task's own instruction, this keeps the task open, materials stay untouched. Two untried, legitimate levers remain: WebP quality tuning (still at the 0.8 default) and frame-count reduction (scales ~linearly under WebP, unlike under PNG where it was already tested and ruled out). Full data in vault/Projects/Website Generator.md, Thread 3 > FORMAT/LAYOUT CELL FILLED + MEASURED LCP + OCCLUSION FLOOR.
  - UPDATED 2026-08-06 (later, control-normalized): re-measured fresh with `measure-lcp.js`'s new control-archetype mechanism (see new DONE task below) - gap is now 1068ms/1316ms (43%/53%) over 2.5s, larger again, but confirmed as real signal, not machine-load drift: the raw gap is more than double this session's largest measured noise spread (648ms), and REVEAL's own ratio-to-control (1.92-2.05x) sits far above every Good-bucket archetype's ratio this same session (0.96-1.24x) - proportionally heavier than the control, not just measured on a slower day. Task stays open correctly; same two untried levers remain, materials still untouched. Full data in vault/Projects/Website Generator.md, Thread 3 > LCP MEASUREMENT METHODOLOGY: CONTROL ARCHETYPE + FULL RE-MEASUREMENT.
- [x] Decide ASSEMBLE/FLYTHROUGH sprite format: both appeared to regress from comfortably Good to clearly Poor under the grid+WebP rework | priority: high | #website-generator
  - RESOLVED 2026-08-06: not a real regression - the old PNG-strip comparison baseline (0.259MB/0.164MB) was corrupted. Root cause found while filling in the missing PNG+grid measurement cell: Playwright's `locator.screenshot()`, the exact mechanism the original pre-rework composite.js used to produce those old figures, silently paints only a small region near the canvas origin and leaves the rest blank white for canvases wider than ~65,535-76,800px in this Chromium build - both ASSEMBLE's (76,800px) and FLYTHROUGH's (86,400px) strip widths exceed that, confirmed directly by sampling frame crops across the full strip (every sampled frame past frame 0 came back pure white). The true PNG-strip size (re-measured via Pillow, calibrated against Chromium's own encoder) is ~1114KB for ASSEMBLE and ~9963KB for FLYTHROUGH - 4.2x and 60.8x the old figures. Re-run against the correct baseline, WebP+grid (already shipped, unchanged) is the smallest option for all six archetypes, not four of six. No pipeline change needed - src/composite.js already only emits WebP. Full data in vault/Projects/Website Generator.md, same section as above.

- [x] Determine whether TRANSFORM's LCP verdict is actually Good, or trim its cost for real headroom (currently a genuine toss-up at the 2.5s line, not a confirmed pass) | priority: medium | #website-generator
  - NEW 2026-08-06: promoted from a "thin margin" watch item to a real task after 15-trial re-measurement (3 runs, both states, control included each time) found TRANSFORM's true value sits close enough to 2.5s that real sessions land on both sides of it - 3 of 6 measured medians landed above the line (2508ms/2644ms/2520ms), 3 below (2432ms/2344ms/2300ms). More trials (5->15) tightened the control's own cross-run spread (228ms->128ms) but did NOT resolve TRANSFORM's verdict - its own cross-run spread (208-212ms) stayed comparable to both the tightened resolution and its original margin, which is the expected signature of a true value sitting right on the boundary, not a measurement-precision problem more trials can fix. Two real options, not yet decided: accept the ambiguity (TRANSFORM might legitimately read Needs Improvement for some real visitors depending on device/network conditions) or trim its actual cost (the grain texture is the one lever already known to move this number - see the 60-unit/luminance-conditional work above) to create real headroom instead of a contested one. Full data in vault/Projects/Website Generator.md, Thread 3 > DOES MORE TRIALS RESOLVE TRANSFORM'S VERDICT?.
  - RESOLVED 2026-08-06 (later, same day): answered by measuring, not by more waiting on noise. Tested the three previously-untried levers independently at 15 trials each with control: WebP quality 0.6 recovers ~204-216ms with no visible texture cost (std 6.06 vs shipped 6.24, screenshot-confirmed); frame count 32->16 recovers ~624-640ms with the texture unaffected and a modest, precedented temporal-smoothness cost (same reduction SPIN already accepted); resolution 1200->900px recovers ~348-364ms but was REJECTED - the hero displays at up to 1200px CSS width on most desktop viewports, so a 900px source gets visibly upscaled ~33%, directly contradicting this project's own established "crisp rendering needs ~2x resolution" finding. Combining quality 0.6 + frame count 16 (resolution untouched) recovers 768-784ms: TRANSFORM lands at 1008ms/1068ms against 2.5s, a ~1.4-1.5s margin - roughly 7x the largest noise figure measured anywhere in this investigation, not a thin margin dressed up as safe. Texture re-confirmed preserved at the combination (std within 4% of shipped, screenshot-checked at both the new midpoint and end frame). Recommended, not yet applied - see new task below for the sign-off/apply step. Full data in vault/Projects/Website Generator.md, Thread 3 > TRANSFORM HEADROOM: THREE UNTRIED LEVERS MEASURED.
  - CORRECTED 2026-08-07: the "same reduction SPIN already accepted" precedent cited above for frame count 32->16 does not exist - checked directly against `src/archetypes.js`, SPIN still ships `frameCount: 24`, unchanged since it was set. SPIN's own 16-frame idea (see the SPIN format-decision entry) was reported and never applied either - "reported, not applied" was misremembered as "accepted" one write-up later. No other archetype has shipped a frame-count cut. Flagged because the very next entry below leaned on this false precedent to justify applying both halves of the fix at once - see that entry's resolution.

- [x] Apply TRANSFORM's measured headroom fix: WebP quality 0.8->0.6 + frame count 32->16 (needs sign-off - a real product change to the shipped archetype, not just a measurement) | priority: medium | #website-generator
  - NEW 2026-08-06: the recommended combination from the task above. Concretely: `src/composite.js`'s `WEBP_QUALITY` constant is project-wide (0.8, shared by all six archetypes) - applying this for TRANSFORM only requires either parametrizing it per-archetype or accepting a project-wide quality drop (not evaluated here, out of scope for the measurement task). `src/archetypes.js`'s TRANSFORM entry needs `frameCount: 16`, `dims` recomputed via the existing `computeGrid`, and `fallbackFrame` moved from 16 to 8 (the equivalent wipe-midpoint index at half the frame count, t=8/15=0.533 vs the original 16/31=0.516). Real fixtures (Birds Barbershop, both states) need regenerating and all guards (sheet-integrity, frustum, occlusion) re-verified after. Not a blind re-apply of the already-tested numbers - re-verify fresh once implemented, the same standard every other change in this document was held to. Full data and the untested fallback option (a smaller frame-count cut, e.g. 24, if 16's temporal choppiness is rejected on inspection) in vault/Projects/Website Generator.md, Thread 3 > TRANSFORM HEADROOM: THREE UNTRIED LEVERS MEASURED.
  - RESOLVED 2026-08-07 (SPLIT DECISION, not the combination above): applied the quality half only; rejected the frame-count half. The frame-count half's justification traced back to a precedent that doesn't exist (see the CORRECTED note above) - with that precedent gone, cutting TRANSFORM's frame count to 16 is a real, disclosed temporal-smoothness cost (animation 2x choppier per step) being paid for LCP margin the archetype doesn't currently need: the idle baseline going into this task already read TRANSFORM at 1780ms/1884ms against 2.5s (620-716ms margin), comfortably Good, not the contested toss-up the task above was opened to resolve. Quality alone is free by comparison - "no visible texture cost, measured" per the same write-up - so it was applied and the frame-count half was not, rather than bundling a real cost in with a free one on the strength of a citation that turned out to be wrong.
  - APPLIED 2026-08-07: `src/composite.js`'s `WEBP_QUALITY` constant lowered 0.8 -> 0.6 (project-wide - it's a single shared constant, confirmed at the time this was scoped; affects all six archetypes' sprite weight, not just TRANSFORM's, see note below). All twelve real fixtures recomposited from their existing captured frames (no recapture needed - only the encode step changes) via a new `test/recomposite-fixtures.js`, and all passed sheet-integrity (built into `compositeSpriteSheet`), frustum, and occlusion re-verification (`test/debug-frustum.js` / `test/debug-occlusion.js`, all six archetypes, all `ok:true`). TRANSFORM re-measured post-change (15 trials, control included): 1760ms/1792ms no-logo/approved-logo, ratio-to-control 1.283x/1.306x - down from the shipped baseline's 1.396x/1.430x same-session ratio, consistent with (not identical to, different session) the original lever test's isolated quality-0.6 reading of 1.23x/1.25x. Margin against 2.5s is now 708-740ms, comfortably Good with room the un-normalized ms figure understates across sessions. Sprite weight dropped project-wide, not just for TRANSFORM: ASSEMBLE -20.7%/-21.4%, REVEAL -21.3%/-22%, SPIN -14.2%/-14.2%, TRANSFORM -25.4%/-27.4%, FLYTHROUGH -22.5%/-21.7%, INTERFACE -9.1%/-12.2% (no-logo/approved-logo). **Not re-measured for LCP**: REVEAL's and ASSEMBLE/FLYTHROUGH's open LCP-gap tasks below still cite their pre-quality-change figures - those figures are now stale by an unmeasured amount (smaller sprites, some LCP recovery likely, gap direction unclear without measuring) and should be re-measured before those tasks are next worked, not assumed closed by this change. (measured: ASSEMBLE 4.72s no-logo / 4.94s approved-logo; FLYTHROUGH 7.84s no-logo / 8.07s approved-logo - both Poor against the 2.5s Good threshold) | priority: high | #website-generator
  - NEW 2026-08-06: closing the format-decision task above was correct (WebP+grid is genuinely each one's smallest available format, not a regression to reverse), but it was the only open item tracking these two, and closing it left the two worst-performing archetypes measured with no open task while REVEAL (3.09s, a smaller gap) has one. Recording accurately, not working it: ASSEMBLE is 2.22s over Good, FLYTHROUGH is 5.34s over - both a materially larger gap than REVEAL's 0.59-0.71s, hence priority: high (above REVEAL's medium). Format is settled and is NOT one of the remaining levers. Levers that remain, untried: frame count (96 for ASSEMBLE, 72 for FLYTHROUGH - both untested for reduction, unlike SPIN/REVEAL where frame-count experiments already ran), resolution (both currently at the standard 1200px-long-edge rule, never re-examined per-archetype), and WebP quality (still the 0.8 default used project-wide, never tuned down for these two specifically). Full data in vault/Projects/Website Generator.md, Thread 3 > FORMAT/LAYOUT CELL FILLED + MEASURED LCP + OCCLUSION FLOOR.
  - UPDATED 2026-08-06 (control-normalized): this was the one open LCP task not yet updated with the control-archetype mechanism's figures - REVEAL's and TRANSFORM's were, this one was orphaned again. Controlled re-measurement (same session as the six-archetype table): ASSEMBLE 5816ms/6068ms no-logo/approved-logo (ratio to control 3.13x/3.26x), FLYTHROUGH 8152ms/8664ms (ratio 4.38x/4.66x) - both far deeper into Poor than the superseded estimate, and both gaps (3.3-4.2s and 5.7-6.2s over the 2.5s line) are an order of magnitude larger than any noise floor measured this project, so the Poor verdict and priority stand without qualification, unlike REVEAL's or TRANSFORM's thinner margins. Separately, a same-session idle-vs-loaded stability check (see vault > RATIO-TO-CONTROL STABILITY UNDER LOAD) found these are specifically the two archetypes where ratio-to-control is NOT reliable across sessions - their ratios moved 0.15-0.25 (in opposite directions from each other) between an idle and a deliberately loaded run, 1.4-2.3x the ratio's own measured noise floor. Doesn't change this task's verdict (the gap is enormous either way) but means: when this task is eventually worked, judge success by the raw ms gap against 2.5s, not by a ratio-to-control comparison across sessions. Full data in vault/Projects/Website Generator.md, Thread 3 > LCP MEASUREMENT METHODOLOGY and RATIO-TO-CONTROL STABILITY UNDER LOAD.
  - CORRECTED 2026-08-06 (later, same day): the mechanism proposed above for WHY these two ratios are unstable (ASSEMBLE decode-dominated via its 92MP grid, FLYTHROUGH network-dominated) was checked against a second independent idle-vs-loaded replication and did not hold up - ASSEMBLE and FLYTHROUGH both showed small, comparable drift the second time (1.0-3.2%), nothing like the first pair's dramatic 12%-vs-2.4% split. Retracted. Real mechanism, confirmed via new transfer/decode decomposition instrumentation in `measure-lcp.js`: both archetypes are actually TRANSFER-dominated (decode is only 2.8-6.5% of their total time), and transfer time is essentially load-independent (≤1.7% movement for every archetype measured, including the control) while decode time is load-sensitive for everyone (+10% to +96%) - the blended ratio's instability tracks each archetype's decode-time SHARE of its total, not pixel count or file size, and that share is noisy session to session because decode times are small absolute numbers. Doesn't change this task's own verdict or priority (the gap remains enormous either way) - only the explanation for the ratio-instability finding, corrected rather than left standing on data that didn't replicate. Full data in vault/Projects/Website Generator.md, Thread 3 > RATIO-TO-CONTROL: THE PX/BYTE MECHANISM DOES NOT REPLICATE.
  - UPDATED 2026-08-06 (later same day): FLYTHROUGH's figures re-measured against the current sheet - the original 7.07s/7.41s were measured against a sheet whose final frame was flat white (the bug fixed in FLYTHROUGH BLANK FRAME FIX + FALLBACK FRAME), nearly free to compress and decode, understating the true cost. Real figures are 7.84s/8.07s, 5.34s over Good (was 4.57s) - still Poor, no bucket change, but the gap this task needs to close is larger than previously recorded. Also found and fixed while re-measuring: the no-logo fixture's metadata.json still claimed the pre-fix sprite's byte count (a real data-integrity bug from the fallbackFrame regeneration step in the previous session, now corrected). Full data in vault/Projects/Website Generator.md, Thread 3 > FORMAT/LAYOUT CELL FILLED + MEASURED LCP + OCCLUSION FLOOR (STALE DATA note) and FLYTHROUGH BLANK FRAME FIX + FALLBACK FRAME.

- [x] Fix FLYTHROUGH's blank final frame: the camera has traveled past all scene geometry by t=1.0, leaving frame 71/72 completely flat white | priority: high | #website-generator
  - RESOLVED 2026-08-06: root cause measured directly (`THREE.Vector3.project()` on ring 5's own tube vertices, not a bounding-sphere frustum test, which gave a false "intersects") - every ring in this scene has a narrow visibility window while the camera approaches it and vanishes once close enough that its own angular size exceeds the ~17.5° half-FOV in every direction at once; the old camera path drove 1.5 units inside that vanished zone for the last ring, with nothing beyond it (no ring 6) to fill the frame. Fixed in `scenes/flythrough.html` by stopping the camera at a measured-safe distance (2.5 units, comfortable margin above the ~1.9-2.0 vanish threshold) instead of a fixed total-travel number. Verified three ways: visual screenshots of the full sequence, per-frame std computed directly (min 43.96 across all 72 frames, no new dead zones), and a full regeneration through the real pipeline confirming `verifySheetIntegrity()` now passes (last frame std=83.257, was 0.000) - all twelve real fixtures now pass. Signboard visibility at `appearsAtFrameFraction` was checked as asked and was never actually the problem - confirmed comfortably framed both before and after this fix, with both a synthetic logo and Mark Fisher Fitness's real asset. Full record in vault/Projects/Website Generator.md, Thread 3 > FLYTHROUGH BLANK FRAME FIX + FALLBACK FRAME.

- [ ] Reconsider the logo fade window (`appearsAtFrameFraction=1.0`, `fadeWindow=0.12`) per archetype - currently hides client branding for 88% of every archetype's runtime with no measured justification | priority: medium | #website-generator
  - NEW 2026-08-06: reported, not applied (per the task's own instruction). Traced both values to a single unreasoned default (a Thread 1 worked-example note describing what it does, not why 1.0 - copied verbatim into `tokens.js` and both `review-gate.js` fallback branches; `fadeWindow` is one hardcoded constant in `scenes/shared.js` shared by all six scenes). Measured occlusion/frustum at multiple points in each sequence with the logo forced visible: TRANSFORM and INTERFACE are clear throughout and could fade in anywhere; FLYTHROUGH is also clear throughout (moving camera, checked with real vertex projection, not assumed) and a `t≈0.45` fade would let the fallback frame be both dynamic (3 rings still visible) and legibly branded - visually confirmed, dissolving the tradeoff the FLYTHROUGH fallbackFrame choice documented; ASSEMBLE needs the fade kept at or after `t≈0.25` (the mounting block isn't built before that - confirmed visually, looks like a rendering glitch, not a design choice, if faded in earlier); REVEAL is a real trap - clear at `t=0` and `t≈0.75+` but badly occluded in between (falling spheres tumble through the signboard's position), so "move it earlier" could make things worse without its own check; SPIN isn't a fade-timing case at all - the label is texture-mapped onto the rotating product, visible only near 0°/360° rotation, confirming `fallbackFrame=0` was doubly correct. Any change needs to be per-archetype, the same way `fallbackFrame` was - not a single global constant edit. Full record in vault/Projects/Website Generator.md, Thread 3 > LOGO FADE WINDOW.

- [ ] Package for end customers (clean file + setup guide, no dev/diag mode visible) | priority: high | #streamer-hud
- [ ] Write customer-facing setup instructions (OBS flag + one-time mic click) | priority: medium | #streamer-hud
- [ ] Decide delivery/sales channel (Gumroad, Etsy, itch.io, own site) | priority: medium | #streamer-hud
- [ ] Write basic licensing terms | priority: low | #streamer-hud

## Done

(move completed tasks here instead of deleting them)

- [x] Fix TRANSFORM's near-blank "after" end state (frame 31, std~4.99) - the animation's own resting frame for every visitor who watches it, not just the fallback | priority: medium | #website-generator
  - DONE 2026-08-06: gave the "after" plane real visual substance (a procedural grain texture, `scenes/transform.html`'s new `makeGrainTexture()`, same CanvasTexture technique as the logo text-wordmark, no new dependency) rather than shortening the animation - rejected not-reaching-t=1.0 since the archetype's whole premise is completing a before/after reveal. Took three measured iterations to land safely: full per-pixel noise at 512px grew the sprite 6.5x (discarded before shipping); a 64px/20-unit-range version was visually fine but barely moved std after WebP compression; a 60-unit-range version gave a clean std improvement but pushed TRANSFORM's measured LCP from 1.04s/1.06s to 2.54s/2.73s - out of Good, caught by re-measuring, not assumed safe - also discarded. Settled at a 40-unit range: sheet grows ~3.3-3.5x (89.6KB/83.1KB to ~298KB/287KB) but LCP stays Good (2.03s/2.12s, 0.38-0.47s of real margin). Re-verified frustum, occlusion, and sheet-integrity guards all still pass with the real client asset, not just the isolated scene test. `fallbackFrame` reconsidered as asked - kept at 16 (the wipe midpoint), not reverted to 31: 16 still uniquely shows both before/after states in one frame, and the texture fix makes it more compelling than before (tactile contrast against the flat "before" half now, not just a color-block cut). Also found and fixed the same metadata-byte-count staleness bug as FLYTHROUGH's (caught before shipping this time) and corrected a factual error in the sheet-integrity guard's own calibration writeup (INTERFACE's frame 3, not TRANSFORM's frame 31, was the true lowest legitimate std - 1.315, not 4.99; the floor value itself was unaffected). Full record in vault/Projects/Website Generator.md, Thread 3 > TRANSFORM END STATE.
  - UPDATED 2026-08-06 (follow-up): grain now scales with the primary color's own luminance instead of applying the full 40-unit range unconditionally - the wash-out was always a light-palette problem (dark bases already get real specular contrast without any grain), so darker clients no longer pay for grain they don't need. Re-verified against two real clients: Birds Barbershop (luma 0.846, light) - sprite ~7-9% smaller (287.4KB/304.7KB -> 263.1KB/274.7KB no-logo/approved-logo), LCP flat within noise measured back-to-back on the same machine; Hiut Denim's real tokens (luma 0.102, dark) re-run through TRANSFORM - frame 31 std=102.9 with barely any grain (range width 4 of 40), confirmed by screenshot the specular sheen alone gives the surface real structure, no wash-out. All twelve fixtures + TRANSFORM's frustum/occlusion guards re-verified passing.
  - UPDATED 2026-08-06 (control-normalized re-test): re-measured the discarded 60-unit-range grain against the shipped luminance-conditional version, back to back, with the new control-archetype mechanism (see new DONE task below) - the 60-unit version still fails (2632ms/2900ms, Needs Improvement) and the discard decision holds, not a noise-driven call: the gap between the two variants (264-592ms) sits at or above this session's own 228ms measurement-noise floor, and the 60-unit run's own control reading was actually faster than the shipped run's, so drift worked against seeing the difference, not for it. Unrequested finding from the same re-test: TRANSFORM's own Good verdict margin is now visibly thin (132-192ms below 2.5s, smaller than this session's measured noise floor) - still measured Good every time this session, but the "0.38-0.47s of real margin" language above overstated the confidence one day's reading can support. Flagged as an open watch item, not a new task at the time. UPGRADED to a real open task 2026-08-06 (later) once 15-trial re-measurement showed it's a genuine toss-up, not just thin - see "Determine whether TRANSFORM's LCP verdict is actually Good" below. Full data in vault/Projects/Website Generator.md, Thread 3 > LCP MEASUREMENT METHODOLOGY: CONTROL ARCHETYPE + FULL RE-MEASUREMENT.

- [x] Add a sheet-integrity guard to the composite stage, so a mostly-blank sprite sheet fails loudly instead of shipping (the blank-canvas bug proved the pre-rework pipeline had no guard at this stage at all) | priority: high | #website-generator
  - DONE 2026-08-06: `src/sheet-integrity.js` + `compose/verify-sheet.html`, wired into `src/composite.js` to run after every write and throw on failure, same pattern as the capture-stage frustum/occlusion guards. Three checks: sheet dimensions match cols*width x rows*height; every frame cell is non-uniform (std-dev floor); adjacent temporal frame pairs aren't overwhelmingly near-identical. The adjacent-pair check was redesigned from a per-pair floor to a fraction-ceiling after real data showed ASSEMBLE/REVEAL have genuine byte-identical static-hold pairs (19/95 and 6/47) that a strict per-pair rule would have false-positived on. Floors calibrated from real fixture data (SPIN's tightest real adjacent diff, TRANSFORM's lowest real per-frame std), not guessed. Unrequested but the most important thing this found: FLYTHROUGH's real, currently-shipped sprite sheet fails the guard right now (see new task above) - not a reconstruction, a live bug caught on the guard's first real run. Verified against the exact historical broken path (reconstructed a 1xN ASSEMBLE strip via `locator.screenshot()`, confirmed rejection, discarded). Full record in vault/Projects/Website Generator.md, Thread 3 > SHEET INTEGRITY GUARD.
  - EXTENDED 2026-08-06: added a fourth, separately-worded check specifically for the caller's `fallbackFrame` (see fallbackFrame task below) - fails as `FALLBACK FRAME IS BLANK` by name rather than being one more index in the generic flat-cell list, since that one frame is the entire hero image for every Firefox/reduced-motion visitor. Verified it fires using the existing broken-strip reconstruction tool; verified all twelve real fixtures still pass with the new check included.
  - CORRECTED 2026-08-06: `NON_UNIFORM_STD_FLOOR` lowered 1.0 -> 0.5. Re-measuring every cell across all six archetypes found the true lowest legitimate std is INTERFACE's earliest frames (0.06-0.06, indices 0-8, std 1.29-1.47) - only a ~1.3x cushion above the old 1.0 floor, not the ~5x originally believed from a wrong TRANSFORM-frame comparison. Looked at INTERFACE frame 3 directly: legitimately sparse, not a bug - the three dashboard panels' staggered slide-in delays mean all three are still off-screen at t~0.06, leaving only a small counter digit visible, confirmed by inspecting the actual frame. Nothing to fix there, so lowered the floor instead (~2.6x margin below the real 1.29 minimum, matching this file's other floors' proportion of headroom) rather than tuning the scene's choreography to pad a test. All twelve fixtures re-verified passing under the corrected floor. Full record in vault/Projects/Website Generator.md, Thread 3 > TRANSFORM GRAIN MADE LUMINANCE-CONDITIONAL + STD FLOOR CORRECTED.

- [x] Fix the LCP measurement methodology: no shared reference point between sessions meant a same-machine, zero-code-change re-measurement of SPIN could drift ~35% against its own stored figure | priority: high | #website-generator
  - DONE 2026-08-06: `measure-lcp.js` now always measures one fixed control archetype (INTERFACE/no-logo - the one archetype with no open task and no code changes planned, and simple/deterministic content) in the same session as whatever else it's asked to measure, reporting every figure's ratio to that session's control reading. Control readings append to a new `test/cost-study/lcp-control-log.json`, structurally separate from `lcp-measured-results.json` so a control check can never again overwrite a real archetype's stored baseline (what happened to SPIN's, requiring a manual revert). New `--control-only` mode for a standalone reading. Re-measured all six archetypes back to back in one session (see vault table) - no verdict bucket changed from the superseded figures, but TRANSFORM's Good margin turned out thinner than believed (see task above) and REVEAL's gap was confirmed real, not drift (see task above). Measured the harness's own resolution directly: 5 control readings across one session spread 1732-1960ms (228ms/13.2% cross-run), with a single 5-trial run's own internal spread ranging 352-548ms - recorded as the smallest difference this harness can currently distinguish from noise, so future single-session comparisons below that aren't overclaimed as signal. Full record in vault/Projects/Website Generator.md, Thread 3 > LCP MEASUREMENT METHODOLOGY: CONTROL ARCHETYPE + FULL RE-MEASUREMENT.

- [x] Make the static/reduced-motion fallback frame an explicit, justified per-archetype choice instead of always defaulting to the last frame | priority: medium | #website-generator
  - DONE 2026-08-06: `fallbackFrame` added to each archetype in `src/archetypes.js`, used by `src/metadata.js` for both the `@supports` unconditional base state and `prefers-reduced-motion` (previously both silently hardcoded the last frame). Defaults to the last frame when unset, so the change is additive. Each value chosen by looking at real candidate frames, not reasoned from code: ASSEMBLE/REVEAL/INTERFACE (95/47/47, last frame - each confirmed the most complete/settled real state by direct inspection), SPIN (0 - the only angle where the product label is actually legible, confirmed frame 12/180° shows no label at all), TRANSFORM (16, the wipe midpoint - shows both before/after simultaneously, confirmed dramatically more informative than either endpoint, the biggest surprise of the pass), FLYTHROUGH (71, last frame - a stated tradeoff: the opening tunnel shot is more visually dynamic but the client's logo has zero opacity before ~88% through the sequence, so no frame is both dynamic and branded; chose brand visibility). Regenerated all twelve real fixtures' metadata/CSS/HTML to match (sprite images unchanged). Full record in vault/Projects/Website Generator.md, Thread 3 > FLYTHROUGH BLANK FRAME FIX + FALLBACK FRAME.

- [x] Fix the occlusion guard's zero-sample hole: `checkOcclusion` returned `visibleRatio=1` (vacuous pass) when a logo produced zero fully-opaque pixels | priority: medium | #website-generator
  - DONE 2026-08-06: `MIN_OPAQUE_PIXEL_COUNT=50` floor added to `scenes/shared.js`, chosen from real fixture measurements - Grace Family Roofing's actual approved PNG logo measures as few as 293 fully-opaque pixels on ASSEMBLE's signboard (soft edges + small on-screen render leave most pixels below full opacity even when genuinely visible), so 50 stays well clear of real production cases while catching true near-zero cases. Below the floor, returns `{ok:false, evaluable:false, visibleRatio:null, reason:"cannot evaluate: ..."}` instead of a silent pass; `src/capture.js` now throws a distinct error for this case (also fixed a latent crash - the old error formatter called `.toFixed()` on what is now `null`). Verified three ways: no regression on all six real/synthetic fixtures, a constructed uniform-alpha=200 synthetic logo now correctly trips the new floor, and the original ASSEMBLE occlusion bug (signboard z=0.55) still correctly trips the existing occlusion-failure path, not the new floor. Full record in vault/Projects/Website Generator.md, Thread 3 > FORMAT/LAYOUT CELL FILLED + MEASURED LCP + OCCLUSION FLOOR.
- [x] Add color/theme customization options to the streamer HUD widget | priority: high | #streamer-hud

- [x] Research brand extraction: pull a client's real colors/fonts/logo from an existing site or brand input, feed as tokens into hero frames + page (Thread 1 of 3) | priority: high | #website-generator
  - DONE 2026-08-05: approved as validated architecture, token pipeline not yet built (diminishing returns on further polish spikes — the mandatory human review gate on logo/accent holds regardless of hit rate). Open product question recorded, not resolved: who performs the review (Fabio vs. client) — see new task above. Full record in vault/Projects/Website Generator.md, Thread 1 section.

- [x] Research the conversion skeleton: proven page structure below the hero (services, social proof, contact/lead flow) that turns visitors into leads (Thread 2 of 3) | priority: medium | #website-generator
  - DONE 2026-08-05: approved as architecture, nothing built. Skeleton, category table, omit-by-default list, and content-input table all adopted. Surfaced a real blocker (no form backend) — see form-backend entry below. Full record in vault/Projects/Website Generator.md, Thread 2 section.

- [x] Settle the form-backend blocker Thread 2 surfaced: generated sites' contact form has nowhere to submit to (static output, no server) | priority: high | #website-generator
  - DECIDED 2026-08-05: Web3Forms adopted now (one account, many generated sites) + a visible mailto: line as zero-cost redundancy + prominent click-to-call as primary, per Thread 2. The proposal's self-hosted Flask-endpoint-relaying-through-Resend design is NOT rejected, just deferred — its cost is ongoing operational responsibility (personal uptime liability for other people's leads) that isn't worth taking on before there are any clients, and orb/app.py currently runs on a workstation that gets restarted routinely, not an always-on host. SWITCH TRIGGER: build the self-hosted endpoint when EITHER 5+ paying clients exist OR the Web3Forms free tier starts capping out — prerequisites at that point are an always-on host for the Flask app and a designed alerting/health-check mechanism (currently undesigned). Full record in vault/Projects/Website Generator.md, Thread 2 → Form Backend section.

- [x] Add a derivation layer to Thread 1's token schema (null fallback per color role, `surface` always derived from `background` with a minimum lightness delta instead of extracted directly, `derived: true/false` flag per token value) | priority: medium | #website-generator
  - DONE 2026-08-05: schema addition, backward-compatible, not a reopening of Thread 1's architecture decision. Prompted directly by two walking-skeleton findings: a null footer color with no defined fallback, and surface/background collapsing to the same value on light-background sites (the majority case per Thread 1's own 9-site validation). Full record in vault/Projects/Website Generator.md, Thread 1 → Derivation Layer section.

- [x] Solve the flipbook sprite-sheet's full-bleed hero scaling gap (Thread 3 prerequisite) | priority: high | #website-generator
  - RESOLVED 2026-08-05: root cause was pixel-based `background-size`, not a limit of the technique. Fix: percentage-based `background-size`/`background-position` (one sprite sheet, no breakpoints needed) + compose the hero as a contained/centered hero-object matching the sprite's native aspect ratio, not literal full-bleed (full-bleed causes a different problem, content distortion, regardless of CSS technique — a design mismatch, not fixable in CSS). Verified across 5 real viewport widths, no bleed, frame-accurate. Vertical sprite orientation tested and rejected (2.56x larger file for identical content). No memory/decode ceiling found up to a real 76,800px-wide source. Real cost: crisp rendering needs ~2x resolution (531KB vs. the original 100.1KB measurement) — stated honestly, does not reverse the CSS-vs-GSAP decision. Thread 3 output spec now defined. Full record in vault/Projects/Website Generator.md, RESOLVED Flipbook Alternative section.

- [x] Decide who performs the mandatory brand-review step (Fabio vs. the client) before the Thread 1 token pipeline is built | priority: high | #website-generator
  - DECIDED 2026-08-05: the CLIENT reviews, not Fabio. Fabio reviewing caps throughput at his own attention and contradicts the "no per-site hand-tuning" premise; the client is better suited anyway (they know their own brand, Fabio would just be guessing). Reframes the mandatory review gate as a "confirm your brand" onboarding feature rather than a visible limitation. Consequence: the token pipeline needs a review UI + correction round-trip, not a fire-and-forget script — the review step must surface the logo, accent/primary color, and detected fonts with an easy correction path, and should visually distinguish `derived: true` values from extracted ones. Also closes the wrong-URL gap the walking skeleton found, for free — a client reviewing their own "confirm your brand" screen would catch an unfamiliar logo/colors extracted from the wrong site. Full record in vault/Projects/Website Generator.md, Thread 1 section.

- [x] Fix the flipbook hero's scroll-track rendering gap (full-page screenshot/PDF export show ~400vh of blank white space between hero and content) | priority: high | #website-generator
  - RESOLVED 2026-08-05: established concretely which contexts break (full-page screenshot composite and print/PDF export — confirmed broken) vs. which don't (normal single-viewport render, most crawlers — confirmed fine, never at risk). Print/PDF: fully fixed via `@media print` collapsing the hero to a normal 100vh static frame — verified, zero blank space. General full-page-screenshot case: no complete fix is possible (no reliable way to detect a non-scrolling composite render, confirmed by investigation) — real mitigation adopted instead, `.hero-track` given a background-color matching the hero's own theme, so the failure mode changed from "jarring blank-white void" to "static themed panel, no motion" — verified by before/after screenshot. Honest constraint stated precisely: no page-level fix can restore the animation in a tool that doesn't emulate real scrolling. Both fixes adopted for the real generator template. Full record in vault/Projects/Website Generator.md, RESOLVED Scroll-track rendering gap section.

- [x] Thread 3 gap: dental/medical doesn't map to any of the six hero archetypes | priority: low | #website-generator
  - RESOLVED 2026-08-05: not a seventh archetype. Dental/medical splits in two — cosmetic/aesthetic practices already fit TRANSFORM (unchanged), and the actual gap (general/family practice) now maps to FLYTHROUGH: a calm environment tour (reception → hallway → treatment room → friendly staff → front desk) that reduces dental/medical anxiety the same way FLYTHROUGH already builds trust for gyms/hotels — ties directly to Thread 2's own finding that booking/insurance/credentials, not visual drama, convert this category, so a slower, quieter FLYTHROUGH pacing is the correct tone. Recommended native aspect ratio: landscape ~16:9 (genre convention for space walkthroughs), contrasting with ASSEMBLE's portrait 2:3 — not independently tested, flagged for Thread 3 to verify when it starts. Full record in vault/Projects/Website Generator.md, Visual Richness → archetype library section.

- [x] Build the real Thread 3 frame-generation pipeline (hybrid template scenes, three.js) for all six archetypes, now that the proposal is approved | priority: medium | #website-generator
  - DONE 2026-08-05 (evening): real (non-spike) pipeline at `tools/frame-generation/` (mirrors `tools/brand-extraction/`'s shape) — six three.js template scenes (ASSEMBLE/REVEAL/SPIN/TRANSFORM/FLYTHROUGH/INTERFACE), headless Playwright capture, canvas-stitched sprite-sheet compositing (no new image-processing dependency), and CSS/HTML snippet output matching the RESOLVED sprite-scaling spec exactly (percentage background-size, `steps()`, `@supports` fallback, `prefers-reduced-motion`). Enforces Recommendation #4's logo-review gate concretely: a real extracted logo only composites when `tokens.meta.reviewStatus === "approved"`; a text-wordmark fallback composites regardless (never an extraction guess). Tested end-to-end against all 9 real Thread 1 fixtures covering all six archetypes, plus the approved-logo and wordmark-fallback paths specifically (visually confirmed). Found and fixed a real bug in testing: several archetypes' logo signboards rendered outside the camera's frustum. Five of six archetype scene designs (all but SPIN) are new for this build, not independently re-verified beyond this pass — full divergence list in vault/Projects/Website Generator.md, new BUILD section in Thread 3. No live integration into the generated-site page template yet (Thread 2 itself has nothing built to wire into).
- [x] Measure Thread 3 sprite-sheet cost (file size, WebP comparison, decode/paint under CPU throttling, LCP implication) before deciding on any optimization | priority: high | #website-generator
  - DONE 2026-08-05 (later same evening): measured, not assumed. 4 of 6 archetypes (ASSEMBLE, TRANSFORM, FLYTHROUGH, INTERFACE) are comfortably Good for LCP as shipped. Found two real problems: REVEAL is 6.01MB, ~7.8x over the Poor LCP threshold even estimated on Slow-4G — confirmed by direct testing this is NOT a frame-count problem (still Poor even at an unusably choppy 8 frames; the sheet is the one archetype where compositing frames together makes the file *bigger* than the frames summed, a real PNG-compressibility problem tied to the glossy material, not frame count) — new task above tracks the fix. SPIN is borderline (2.98s, just over Good) and frame-count reduction genuinely works there (16 frames tested at ~2.02s) — new task above tracks the sign-off needed before applying it. Also found a structural WebP blocker affecting all six archetypes: a single horizontal sprite sheet exceeds WebP's hard 16383px dimension limit at every archetype's current width, so "just re-encode as WebP" isn't available as a drop-in fix for any of them without a sheet-layout change (grid instead of strip). Full data and method in vault/Projects/Website Generator.md, Thread 3 > COST STUDY.
- [x] Add a frustum regression guard to Thread 3 capture, so an out-of-view logo signboard fails the capture loudly instead of shipping silently | priority: high | #website-generator
  - DONE 2026-08-05 (later same evening): `FrameScene.checkFrustum` (scenes/shared.js) projects the logo mesh's bounding-box corners through the camera and asserts they're within a 5% NDC margin; `src/capture.js` now runs this on every capture and throws if it fails. Running it for the first time found 5 of 6 archetypes failing, not just the ASSEMBLE case flagged going in — all four others were genuinely retuned and re-verified (not margin-loosened). ASSEMBLE needed two attempts: the first retune passed the frustum check numerically but visual inspection caught the wordmark had become fully invisible, occluded behind a tower block — a real, disclosed limit of a frustum-only check (it can't see occlusion by other scene geometry). Second attempt (moved the signboard in front of the block's actual face) is a genuine fix, confirmed both by the guard and by looking at the actual rendered frame. All six archetypes pass now; none left in a reported-still-failing state. Full record in vault/Projects/Website Generator.md, Thread 3 > FRUSTUM REGRESSION GUARD.
- [x] Rework Thread 3's sprite sheet to a 2D grid + WebP, so it can actually use WebP (the old 1xN strip exceeded libwebp's 16383px limit for every archetype) | priority: high | #website-generator
  - DONE 2026-08-06: `src/archetypes.js` computes a near-square cols x rows grid per archetype; `compose/composite.html` lays frames out in it and encodes via the browser's own `canvas.toDataURL('image/webp')` (no new dependency); `src/metadata.js` replaced `steps()` with explicit per-frame `@keyframes` (2-axis background-position, `steps(1, jump-end)` per stop) since a single-axis step function doesn't generalize to a grid. Verified pixel-for-pixel against a real captured frame, not just by construction. Re-measured all six archetypes in both no-logo and approved-logo states: REVEAL improved 10.8x (31.3s -> ~2.9-3.0s) but doesn't fully clear Good (see task above) — materials untouched, per instruction. SPIN's earlier borderline problem is resolved by format alone at 24 frames (no cut needed). ASSEMBLE and FLYTHROUGH regressed from Good to Poor — new task above, undecided. Found and fixed a real robustness gap along the way: a failed/zero-size logo texture load used to hang for a silent 30s timeout; now fails in ~2s with a specific error (this is what caught Family Law in Partnership's pre-existing broken SVG logo, a Thread 1 issue substituted around for this measurement, not fixed here). Full record in vault/Projects/Website Generator.md, Thread 3 > GRID SPRITE LAYOUT + WEBP.
- [x] Upgrade the frustum guard to also catch occlusion by other scene geometry (the frustum check alone already proved it could pass while a logo was fully hidden) | priority: high | #website-generator
  - DONE 2026-08-06: `FrameScene.checkOcclusion` renders the logo frame twice (normal, and with everything else hidden) and compares the logo's own pixel footprint between them; enforced in `src/capture.js` alongside the frustum check. Found and fixed a real false positive along the way, not just a clean implementation: the first version (color-distance from a background reference) failed on its very first real-asset test — Grace Family Roofing's actual white-on-transparent logo over a white scene background made the logo's own pixels indistinguishable from background by color, reporting 0% visible on a logo confirmed fully visible by screenshot. Fixed by reading the texture's alpha channel directly instead of guessing from color (nulling the scene background for the isolated pass, so only the logo mesh can produce alpha there) — surfaced a deeper, non-tunable finding in the process: only fully-opaque pixels (alpha>=250) can be validly compared this way at all, since alpha-blending math makes a partially-transparent pixel's rendered color depend on what's behind it regardless of any real occlusion. Verified against the original ASSEMBLE failure specifically, per instruction: reconstructed the known-bad placement, confirmed the guard now rejects it (both with a synthetic logo and Grace Family Roofing's real one), reverted. All six archetypes pass at their current (correct) placements. Full record in vault/Projects/Website Generator.md, Thread 3 > OCCLUSION REGRESSION GUARD.
- [x] Research frame generation for the flipbook archetypes: evaluate AI text-to-video->frames vs. real 3D render->export vs. hybrid template scenes; accuracy is non-negotiable for clients wanting their actual building/product (Thread 3 of 3) | priority: medium | #website-generator
  - DONE 2026-08-05: PROPOSAL, APPROVED by Fabio same evening — same status Thread 1/2 already reached. Post-approval attempt to hands-on test AI text-to-video (option a) via Higgsfield MCP was blocked by a paid-plan gate (`job_minimum_basic_plan_required`), not credits — option (a)'s rejection stands on researched-not-tested grounds. Recommendation splits by archetype rather than one universal winner: hybrid template scenes (three.js, already-vendored, zero new dependency) as the default for SPIN/REVEAL/TRANSFORM/INTERFACE, validated by a spike (`spikes/frame-generation/template_spin.py`) showing a fixed template + per-client color/logo params produces correct, distinct output in ~4ms/frame with no hand-tuning. AI text-to-video (option a) rejected outright — current tools solve character consistency, not exact-geometry/camera-path consistency, and no API budget was authorized to test hands-on anyway. Real 3D capture (option b) is accurate but inherently manual (photogrammetry cleanup can't be scripted away) and costly ($3k-$10k+/2-4wk professional-grade, sourced; cheaper DIY estimated, not sourced) — scoped as a paid add-on, not the default. Honest unresolved tension: ASSEMBLE and FLYTHROUGH are the two archetypes where template abstraction genuinely fails the "client will notice a generic tower" bar (the geometry itself is the client-specific content, not an incidental carrier for their colors) — recommended to ship generic/disclosed-as-stylized by default with real capture as a paid upgrade for those two specifically, not solved cleanly. Aspect-ratio open item resolved: ASSEMBLE 2:3 (unchanged, tested) and FLYTHROUGH 16:9 (unchanged) stand; new reasoned recommendations added for REVEAL (4:5), SPIN (1:1), TRANSFORM (3:2), and INTERFACE (16:9+) from each archetype's actual content/genre convention, not independently tested end-to-end. Full record in vault/Projects/Website Generator.md, new Thread 3 section (after Sources (Visual Richness), before UI Craft).

- [ ] ❌ Validate pricing and positioning against direct competitors on Fiverr/Upwork | priority: high | cancelled: 2026-07-29 | #jarvis-as-a-service
- [ ] ❌ Build a portfolio/demo site showcasing the orb dashboard | priority: high | cancelled: 2026-07-29 | #jarvis-as-a-service
- [ ] ❌ Draft the actual gig/listing copy | priority: medium | cancelled: 2026-07-29 | #jarvis-as-a-service
- [ ] ❌ Record a 3-5 min demo video of the orb dashboard and agent delegation in action | priority: high | cancelled: 2026-07-29 | #jarvis-as-a-service
- [ ] ❌ Decide target buyer: individual assistant-seekers vs. small-business automation clients | priority: high | cancelled: 2026-07-29 | #jarvis-as-a-service
- [ ] ❌ Scope a productization checklist (auth, config isolation, docs) before quoting client work | priority: medium | cancelled: 2026-07-29 | #jarvis-as-a-service
- [ ] ❌ Build v1 static demo site (screen-recording hero, no live backend) once the recording exists | priority: medium | cancelled: 2026-07-29 | #jarvis-as-a-service

